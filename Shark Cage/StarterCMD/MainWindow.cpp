#include "stdafx.h"
/*
#include "MainWindow.h"


MainWindow::MainWindow(QWidget *parent) : QMainWindow(parent), ui(new Ui::MainWindow)
{
    ui->setupUi(this);
    setWindowTitle(tr("Fractal painter"));
    fraktalRenderArea = new RenderArea(this);
    setCentralWidget(fraktalRenderArea);
    setWindowState(Qt::WindowMaximized);
    numFract = Mandelbrot;
    useCPU = true;
    //numColorMode = 0;

    // Set menu
    // file menu
    QMenu *fileMenu = new QMenu(tr("&File"), this);


    QButton btn(...); // Add the action with signals/slots
    // mgr.send(MSG_TO_SERVICE_toString(START_CM));

    mgr = NetworkManager(UI);
}

MainWindow::~MainWindow()
{
}
*/